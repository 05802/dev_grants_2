# Local Development (for wrangler pages dev)
# Copy to .dev.vars for local testing

OPENAI_API_KEY=sk-your-openai-api-key
ANTHROPIC_API_KEY=sk-ant-your-anthropic-key

# In production: set these in Cloudflare Dashboard → Settings → Environment Variables
